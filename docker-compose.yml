version: '3.8'

services:
  db:
    image: postgres:15-alpine
    container_name: blog_postgres
    environment:
      POSTGRES_USER: sofa
      POSTGRES_PASSWORD: burc121005
      POSTGRES_DB: blogdb
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata: